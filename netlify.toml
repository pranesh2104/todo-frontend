[dev]
  command = "npm run start"
  targetPort = 5200
  port = 8888 
  publish = "dist"

[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist/todo/browser"


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true