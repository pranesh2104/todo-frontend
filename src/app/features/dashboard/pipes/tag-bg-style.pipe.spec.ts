import { TagBgStylePipe } from './tag-bg-style.pipe';

describe('TagBgStylePipe', () => {
  it('create an instance', () => {
    const pipe = new TagBgStylePipe();
    expect(pipe).toBeTruthy();
  });
});
